go.work
go.work.sum
# No trailing slash — in worktrees these are symlinks, and git's
# pattern/ syntax only matches directories, not symlinks-to-directories.
.agent-history
.ralph-ban
.cloned-sources
.beads-lite
.claude/agents
ralph-ban
